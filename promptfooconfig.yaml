description: Multi-model sanity test - sky color (OpenAI + Claude)

prompts:
  - "What is the color of the sky?"

providers:
  - id: openai:chat:gpt-4.1-mini
    config:
      temperature: 0

  - id: anthropic:messages:claude-sonnet-4-20250514
    config:
      temperature: 0

tests:
  - assert:
      - type: contains
        value: blue
